version: "2"
services:
  web:
    build: ../
    ports:
     - "${APP_PORT}:${APP_PORT}"
    restart: always
    environment:
      - REDIS_URL=redis://redis:6379
    env_file:
      - configuration
      - ../environment_variables.env
